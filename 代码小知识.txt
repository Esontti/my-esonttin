代码公式
1、最大公约数（GCD）的求解（辗转相除法）
	int gcd(int a, int b)
	{
		return b ? gcd(b, a % b) : a;
	}
2、年份处理
	1）求闰年：y%400==0 || (y%4==0&&y%100!=0)
	2）
3、奇偶数的区分
	1）if（i &1 ）为奇数
	2）if（i %2==1）//为奇数，i %2==0 为偶数 
4、代码的四舍五入
	 float pass;//及格率
	 pass=(float)passsum/sum*100;
	 int  ans=( int)( pass+0. 5);